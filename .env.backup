# Configuración del dominio - ACTUALIZA CON TU DOMINIO
DOMAIN=tu-dominio.com
FRONTEND_URL=https://tu-dominio.com
BACKEND_URL=https://api.tu-dominio.com

# Puertos
API_PORT=3000
GAME_PORT=3001

# Configuración de la base de datos
DB_HOST=localhost
DB_PORT=5432
DB_NAME=agar_betting
DB_USER=tu_usuario
DB_PASSWORD=tu_password

# Configuración de JWT
JWT_SECRET=tu_jwt_secret_super_seguro_cambia_esto

# Configuración de Google OAuth (si usas)
GOOGLE_CLIENT_ID=tu_google_client_id
GOOGLE_CLIENT_SECRET=tu_google_client_secret

# Configuración de CORS para el VPS
CORS_ORIGINS=http://localhost:3000,https://tu-dominio.com,https://www.tu-dominio.com

# Node Environment
NODE_ENV=production
